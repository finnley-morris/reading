import React from "react";
import { useState } from "react";

// radio buttons representing each letter on the grid.
// rules: letters need to be selected from left to right in order
// if selected out of order, reset the word
// check each letter is the letter expected next in the word
// if an incorrect letter is selected, reset the word
// if the word is completed, display a message

// one word will be prompted at a time
// if the word is completed, prompt the next word





const WordSearch = () => {


    return(
        <div>
            <h1>Word Search </h1>
        </div>
    )
}

export default WordSearch;